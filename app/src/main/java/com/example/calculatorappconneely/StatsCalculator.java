package com.example.calculatorappconneely;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.util.Log;

public class StatsCalculator extends AppCompatActivity {
    /* TODO: UN-patch this.
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_stats_fxns);
    }
    // open the intent to get the data when screen loads
    Intent intent = getIntent();
    String receivedFullText = intent.getStringExtra("FULLNAME");
    // log to see what was received
    Log.i("denna", "received: "+ receivedFullText);

     */
}
